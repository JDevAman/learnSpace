# GIT

## DOCS

Link: [Git Doc](https://git-scm.com/docs/)

## COMMANDS

- New Branch: git checkout -b <branch_name>
- Branch: git checkout <branch_name>
- Delete Branch: git branch -d <branch_name>
- Show branch: git branch
- Add Remote Origin: git remote add origin <url>
- Set Remote Origin: git remote set-url origin <url>

## Scenarios

1. Merge Confilct
   When pulling from remote, your local and remote are not merging due to unrelated history.
   Ans: First Time Repo: set flag --allow-unrelated-histories
